// The app icons are generated from the Chapter1 logo artwork, not drawn here.
// public/logo.png and public/icons/icon-{192,512}.png are produced by keying the
// mark out of the source render (glow preserved) and compositing onto the app
// background (#16121f). Re-run that pipeline with a new source image if the
// logo changes — this stub exists so the old placeholder-icon generator can't
// silently overwrite the real branding.
console.error('make-icons.mjs is retired: icons are derived from the Chapter1 logo artwork (public/logo.png).');
process.exit(1);
